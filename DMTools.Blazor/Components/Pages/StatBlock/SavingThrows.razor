@if (this.savingThrows.NumOfSavingThrows > 0)
{
	<div class="property-line">
		<h4>Saving Throws</h4>
		<p>
			@savingThrows.ToString()
		</p>
	</div> <!-- property line -->
}

@code {
	[Parameter]
	public DMToolsLibrary.StatBlocks.StatBlockHelpers.SavingThrows savingThrows { get; set; }
}
