﻿@if (this.skills.Count > 0)
{
	<div class="property-line">
		<h4>Skills</h4>
		<p>
			@string.Join(", ", this.skills.Select(skill => skill.ToString()))
		</p>
	</div> <!-- property line -->
}

@code {
	[Parameter]
	public ICollection<DMTools.Database.Entities.Skills> skills { get; set; }
}